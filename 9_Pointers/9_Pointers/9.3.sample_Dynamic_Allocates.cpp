// This program finds the average of a set of grades.
// It dynamically allocates space for the array holding the grades.
#include <iostream>
#include <iomanip>
using namespace std;
// function prototypes
int main()
{
	int x = 2, y = 1, z = 0;
	cout << "answer = " << (2 || !1 && 0) << endl;;
		
		system("pause");
		return 0;
}
